import Container from "../ui/container"

const About = () => {
    return (
        <section id="about" className="min-h-screen flex items-center overflow-hidden" aria-label="About section">
            <Container>About</Container>
        </section>
    )
}

export default About
