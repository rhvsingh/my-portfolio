import Container from "../ui/container"

const Resume = () => {
    return (
        <section id="resume" className="min-h-screen flex items-center overflow-hidden" aria-label="Resume section">
            <Container>Resume</Container>
        </section>
    )
}

export default Resume
