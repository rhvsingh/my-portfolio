import Container from "../ui/container"

const Projects = () => {
    return (
        <section id="projects" className="min-h-screen flex items-center overflow-hidden" aria-label="Projects section">
            <Container>Projects</Container>
        </section>
    )
}

export default Projects
